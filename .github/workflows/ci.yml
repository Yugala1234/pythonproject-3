name: CI for Project 2

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  call-reusable:
    uses: your-org/ci-workflows/.github/workflows/python-ci.yml@main
    with:
      python-version: "3.11"
      requirements-file: "requirements.txt"
